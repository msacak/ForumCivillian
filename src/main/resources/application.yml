server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/forum_civillian_db
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: localhost
    port: 25 #smptp4dev

